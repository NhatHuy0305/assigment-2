package com.example.calculator;
import com.example.calculator.SIGN;

public class ImplementMultiply {
    public Double calculate(SIGN sign, Double arg1, Double arg2) {
        switch (sign) {
            case MULTIPLY:
                return arg1 * arg2;
            default:
                break;
        }
        return 0d;
    }
}
